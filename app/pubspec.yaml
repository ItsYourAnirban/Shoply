name: shoply
description: Shoply — One search, all stores, best price.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.2
  url_launcher: ^6.3.0
  geolocator: ^13.0.1
  intl: ^0.19.0

flutter:
  uses-material-design: true
  assets: []
